# Practice Tasks

## Done

- [x] Search by city name
- [x] Debounce live search
- [x] Save result to history
- [x] Delete from history
- [x] Reload a history item with fresh data
- [x] Error handling

## Next

- [ ] **Loading state per history item** — show a spinner on the reload button while that specific item is reloading
- [ ] **Prevent duplicate saves** — disable or warn if the same city is already in history
- [ ] **Sort history** — add buttons to sort history by city name or by date
- [ ] **Clear all history** — add a "clear all" button
- [ ] **Persist history** — save history to localStorage so it survives page refresh
- [ ] **Limit history** — cap history at 10 items, remove oldest when limit is exceeded
- [ ] **Extract components** — move the search card and history item into their own components in separate files
